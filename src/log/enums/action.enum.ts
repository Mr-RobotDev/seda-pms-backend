export enum Action {
  LOGGED_IN = 'logged in',
  LOGGED_OUT = 'logged out',
  CREATED = 'created',
  VIEWED = 'viewed',
  UPDATED = 'updated',
  DELETED = 'deleted',
}

export const ActionValues = Object.values(Action);
