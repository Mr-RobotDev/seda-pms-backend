export enum Page {
  DASHBOARD = 'dashboard',
  FLOOR_PLAN = 'floor plan',
  ALERTS = 'alerts',
  DATA_SOURCES = 'data sources',
  DEVICES = 'devices',
  DEVICE = 'device',
}

export const PageValues = Object.values(Page);
